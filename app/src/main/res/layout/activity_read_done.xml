<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F5EFE6">

    <!-- 🔹 스크롤 영역 -->
    <ScrollView
        android:id="@+id/scrollArea"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:fillViewport="true"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/layout_bottom_menu"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp">

            <!-- ===== 책 커버 (2:3 비율) ===== -->
            <androidx.cardview.widget.CardView
                android:id="@+id/book_cover_card_view"
                android:layout_width="88dp"
                android:layout_height="0dp"
                app:cardCornerRadius="8dp"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                >

                <ImageView
                    android:id="@+id/iv_book_cover"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:scaleType="centerCrop"
                    android:adjustViewBounds="true"
                    android:contentDescription="book cover"
                    android:src="@drawable/demian_book"
                    app:layout_constraintDimensionRatio="2:3" />


            </androidx.cardview.widget.CardView>

            <!-- 📘 제목 -->
            <TextView
                android:id="@+id/bookTitle"
                android:layout_width="0dp"
                android:layout_height="28dp"
                android:text="인간 실격"
                android:textSize="14sp"
                android:textStyle="bold"
                android:maxLines="1"
                android:ellipsize="end"
                android:textColor="#000000"
                android:fontFamily="@font/noto_serif_kr_semi_bold"
                app:layout_constraintTop_toTopOf="@id/book_cover_card_view"
                app:layout_constraintStart_toEndOf="@id/book_cover_card_view"
                app:layout_constraintEnd_toEndOf="parent"
                android:layout_marginStart="12dp"/>

            <!-- 📘 메타 정보 -->
            <TextView
                android:id="@+id/bookAuthor"
                android:layout_width="wrap_content"
                android:layout_height="20dp"
                android:text="다자이 오사무"
                android:fontFamily="@font/noto_serif_kr_regular"
                android:textSize="10sp"
                android:textColor="#6F6F6F"
                android:maxLines="1"
                android:ellipsize="end"
                app:layout_constraintTop_toBottomOf="@id/bookTitle"
                app:layout_constraintStart_toStartOf="@id/bookTitle"/>

            <TextView
                android:id="@+id/bookScore"
                android:layout_width="wrap_content"
                android:layout_height="24dp"
                android:text="85 / 100"
                android:fontFamily="@font/noto_serif_kr_semi_bold"
                android:textColor="#000000"
                android:textSize="12sp"
                app:layout_constraintTop_toBottomOf="@id/bookAuthor"
                app:layout_constraintStart_toStartOf="@id/bookTitle"/>

            <TextView
                android:id="@+id/bookComment"
                android:layout_width="wrap_content"
                android:layout_height="24dp"
                android:text="독서 완료"
                android:fontFamily="@font/noto_serif_kr_semi_bold"
                android:textSize="12sp"
                android:textColor="#000000"
                android:ellipsize="end"
                app:layout_constraintTop_toBottomOf="@id/bookScore"
                app:layout_constraintStart_toStartOf="@id/bookTitle"/>

            <!-- 🔹 섹션 타이틀 -->
            <TextView
                android:id="@+id/readLogTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="내가 작성한 독서 일지를 돌아보세요"
                android:textColor="#000000"
                android:fontFamily="@font/noto_serif_kr_bold"
                app:layout_constraintTop_toBottomOf="@id/book_cover_card_view"
                app:layout_constraintStart_toStartOf="parent"
                android:layout_marginTop="16dp"/>

            <!-- 🔹 보기 방식 버튼 -->
            <LinearLayout
                android:id="@+id/viewModeButtons"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:orientation="horizontal"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/readLogTitle">

                <Button
                    android:id="@+id/btnSlideView"
                    android:layout_width="wrap_content"
                    android:layout_height="40dp"
                    android:paddingStart="8dp"
                    android:paddingEnd="8dp"
                    android:background="@drawable/bg_tab_selected"
                    android:backgroundTint="#CBBBA0"
                    android:text="슬라이드로 보기"
                    android:textSize="12sp"
                    android:fontFamily="@font/noto_serif_kr_regular"
                    android:textColor="@color/black"/>

                <Button
                    android:id="@+id/btnPageView"
                    android:layout_width="wrap_content"
                    android:layout_height="40dp"
                    android:background="@drawable/bg_tab_selected"
                    android:backgroundTint="#E9E1D6"
                    android:layout_marginStart="8dp"
                    android:text="좌우로 보기"
                    android:textSize="12sp"
                    android:fontFamily="@font/noto_serif_kr_regular"
                    android:textColor="@color/black"/>

            </LinearLayout>

            <!-- 🔹 독서 일지 컨테이너 -->
            <FrameLayout
                android:id="@+id/readLogContainer"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/viewModeButtons">

                <!-- 슬라이드 모드 -->
                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/read_log_recyclerView"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:clipToPadding="false"
                    nestedScrollingEnabled="false"
                    android:visibility="visible" />

                <!-- 좌우 페이지 모드 -->
                <androidx.viewpager2.widget.ViewPager2
                    android:id="@+id/read_log_viewPager"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:visibility="gone" />
            </FrameLayout>

        </androidx.constraintlayout.widget.ConstraintLayout>
    </ScrollView>

    <!-- =======================
         하단 메뉴 영역
    ======================== -->
    <LinearLayout
        android:id="@+id/layout_bottom_menu"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:orientation="horizontal"
        android:gravity="center"
        android:background="#E9E1D6"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageButton
            android:id="@+id/btn_menu_search"
            style="@style/BottomMenuButton"
            android:src="@drawable/search_32px"
            android:background="@drawable/bg_under_btn_bar"
            android:backgroundTint="#E9E1D6"/>

        <ImageButton
            android:id="@+id/btn_menu_library"
            style="@style/BottomMenuButton"
            android:src="@drawable/book_32px"
            android:background="@drawable/bg_under_btn_bar"
            android:backgroundTint="#CBBBA0" />

        <ImageButton
            android:id="@+id/btn_menu_home"
            style="@style/BottomMenuButton"
            android:src="@drawable/home_32px"
            android:background="@drawable/bg_under_btn_bar"
            android:backgroundTint="#E9E1D6" />

        <ImageButton
            android:id="@+id/btn_menu_record"
            style="@style/BottomMenuButton"
            android:src="@drawable/note_32px"
            android:background="@drawable/bg_under_btn_bar"
            android:backgroundTint="#E9E1D6"/>

        <ImageButton
            android:id="@+id/btn_menu_mypage"
            style="@style/BottomMenuButton"
            android:src="@drawable/profile_32px"
            android:background="@drawable/bg_under_btn_bar"
            android:backgroundTint="#E9E1D6"/>

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
