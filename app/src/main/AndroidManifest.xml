<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".Util.ApplicationUtil"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RememberBooks"
        android:networkSecurityConfig="@xml/network_security_config">

        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="ocr" />

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-9631098870024659~6572607508"/>

        <activity
            android:name=".Activity.MainActivity"
            android:exported="true">
        </activity>
        <activity android:name=".Activity.MyLibraryActivity"/>
        <activity android:name=".Activity.ReadDoneRecordActivity"/>
        <activity android:name=".Activity.ReadingRecordActivity"/>
        <activity android:name=".Activity.SearchBookActivity"/>
        <activity android:name=".Activity.SearchBookListActivity"/>
        <activity android:name=".Activity.MyPageActivity"/>
        <activity android:name=".Activity.MySettingActivity"/>
        <activity android:name=".Activity.GetTextFromImgActivity"/>
        <activity android:name=".Activity.TalkWithAIActivity"/>
        <activity
            android:name=".Activity.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <!-- 리다이렉트 URI: "kakao${NATIVE_APP_KEY}://oauth" -->
                <data android:host="oauth"
                    android:scheme="kakao${kakaoNativeAppKey}" />
            </intent-filter>
        </activity>

    </application>

</manifest>